<div class="overflow-hidden bg-white shadow sm:rounded-lg w-full">
  <div class="border-b border-gray-200">
    <div class="sm:hidden px-4 py-3">
      <label class="sr-only" for="tab-select">Select a tab</label>
      <select
        id="tab-select"
        class="block w-full rounded-md border-gray-300 py-2 pl-3 pr-10 text-sm focus:border-indigo-500 focus:outline-none focus:ring-indigo-500"
        onchange="switchTab(this.value)"
      >
        <option value="tier-standard">Standard</option>
        <option value="tier-premium">Premium</option>
        <option value="summary" selected>Summary</option>
        <option value="topics">Topics</option>
        <option value="highlights">Highlights</option>
        <option value="detailed">Detailed Notes</option>
        <option value="actions">Actions</option>
        <option value="followups">Follow-ups</option>
        <option value="transcript">Transcript</option>
        <option value="stats">Stats</option>
        <option value="media">Media</option>
        <option value="quickactions">Quick Actions</option>
      </select>
    </div>
    <div class="hidden sm:block w-full">
      <div class="flex flex-wrap items-center gap-3 px-4 py-3 border-b border-gray-100">
        <button
          type="button"
          onclick="switchTier('standard')"
          id="tier-standard"
          data-tier-target="standard"
          class="tier-btn inline-flex items-center rounded-full border border-indigo-500 bg-white px-3 py-1 text-xs font-semibold text-indigo-600"
          aria-selected="true"
        >
          Standard
        </button>
        <button
          type="button"
          onclick="switchTier('premium')"
          id="tier-premium"
          data-tier-target="premium"
          class="tier-btn inline-flex items-center rounded-full border border-transparent bg-white px-3 py-1 text-xs font-semibold text-gray-500"
          aria-selected="false"
          tabindex="-1"
        >
          Premium
        </button>
        <span class="text-xs text-gray-500">Standard uses Recall/OpenAI · Premium uses AssemblyAI</span>
      </div>
      <!-- Standard Tabs Navigation -->
      <div data-tier="standard" class="w-full overflow-x-auto scrollbar-hide">
      <nav class="-mb-px flex w-full flex-wrap sm:flex-nowrap" aria-label="Standard Tabs" role="tablist">
        <button
          type="button"
          onclick="switchTab('summary', 'standard')"
          id="tab-standard-summary"
          role="tab"
          aria-controls="content-summary"
          aria-selected="true"
          tabindex="0"
          class="tab-btn-standard group relative flex-1 min-w-0 border-indigo-500 text-indigo-600 whitespace-nowrap border-b-2 py-3 px-2 sm:px-3 text-center text-xs sm:text-sm font-medium transition-colors"
        >
          <svg class="mx-auto h-4 w-4 sm:h-5 sm:w-5 mb-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"></path>
          </svg>
          Summary
        </button>
        <button
          type="button"
          onclick="switchTab('topics', 'standard')"
          id="tab-standard-topics"
          role="tab"
          aria-controls="content-topics"
          aria-selected="false"
          tabindex="-1"
          class="tab-btn-standard group relative flex-1 min-w-0 border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 whitespace-nowrap border-b-2 py-3 px-2 sm:px-3 text-center text-xs sm:text-sm font-medium transition-colors"
        >
          <svg class="mx-auto h-4 w-4 sm:h-5 sm:w-5 mb-0.5 group-hover:text-indigo-500 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 10h16M4 14h10"></path>
          </svg>
          Topics
        </button>
        <button
          type="button"
          onclick="switchTab('highlights', 'standard')"
          id="tab-standard-highlights"
          role="tab"
          aria-controls="content-highlights"
          aria-selected="false"
          tabindex="-1"
          class="tab-btn-standard group relative flex-1 min-w-0 border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 whitespace-nowrap border-b-2 py-3 px-2 sm:px-3 text-center text-xs sm:text-sm font-medium transition-colors"
        >
          <svg class="mx-auto h-5 w-5 mb-0.5 group-hover:text-indigo-500 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
          </svg>
          Highlights
        </button>
        <button
          type="button"
          onclick="switchTab('detailed', 'standard')"
          id="tab-standard-detailed"
          role="tab"
          aria-controls="content-detailed"
          aria-selected="false"
          tabindex="-1"
          class="tab-btn-standard group relative flex-1 min-w-0 border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 whitespace-nowrap border-b-2 py-3 px-2 sm:px-3 text-center text-xs sm:text-sm font-medium transition-colors"
        >
          <svg class="mx-auto h-5 w-5 mb-0.5 group-hover:text-indigo-500 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 10h.01M12 10h.01M16 10h.01M9 16H5a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v8a2 2 0 01-2 2h-5l-4 4v-4z"></path>
          </svg>
          <span class="hidden sm:inline">Detailed Notes</span>
          <span class="sm:hidden">Notes</span>
        </button>
        <button
          type="button"
          onclick="switchTab('actions', 'standard')"
          id="tab-standard-actions"
          role="tab"
          aria-controls="content-actions"
          aria-selected="false"
          tabindex="-1"
          class="tab-btn-standard group relative flex-1 min-w-0 border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 whitespace-nowrap border-b-2 py-3 px-2 sm:px-3 text-center text-xs sm:text-sm font-medium transition-colors"
        >
          <svg class="mx-auto h-5 w-5 mb-0.5 group-hover:text-indigo-500 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-6 9l2 2 4-4"></path>
          </svg>
          Actions
          <% if (meeting.actionItems && meeting.actionItems.length > 0) { %>
            <span class="absolute -top-1 -right-1 inline-flex items-center justify-center h-5 w-5 rounded-full bg-orange-500 text-xs font-bold text-white"><%= meeting.actionItems.length %></span>
          <% } %>
        </button>
        <button
          type="button"
          onclick="switchTab('followups', 'standard')"
          id="tab-standard-followups"
          role="tab"
          aria-controls="content-followups"
          aria-selected="false"
          tabindex="-1"
          class="tab-btn-standard group relative flex-1 min-w-0 border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 whitespace-nowrap border-b-2 py-3 px-2 sm:px-3 text-center text-xs sm:text-sm font-medium transition-colors"
        >
          <svg class="mx-auto h-5 w-5 mb-0.5 group-hover:text-indigo-500 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 9l3 3m0 0l-3 3m3-3H8m13 0a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
          <span class="hidden sm:inline">Follow-ups</span>
          <span class="sm:hidden">Follow</span>
          <% if (meeting.followUps && meeting.followUps.length > 0) { %>
            <span class="absolute -top-1 -right-1 inline-flex items-center justify-center h-5 w-5 rounded-full bg-blue-500 text-xs font-bold text-white"><%= meeting.followUps.length %></span>
          <% } %>
        </button>
        <button
          type="button"
          onclick="switchTab('transcript', 'standard')"
          id="tab-standard-transcript"
          role="tab"
          aria-controls="content-transcript"
          aria-selected="false"
          tabindex="-1"
          class="tab-btn-standard group relative flex-1 min-w-0 border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 whitespace-nowrap border-b-2 py-3 px-2 sm:px-3 text-center text-xs sm:text-sm font-medium transition-colors"
        >
          <svg class="mx-auto h-5 w-5 mb-0.5 group-hover:text-indigo-500 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
          </svg>
          Transcript
        </button>
        <button
          type="button"
          onclick="switchTab('stats', 'standard')"
          id="tab-standard-stats"
          role="tab"
          aria-controls="content-stats"
          aria-selected="false"
          tabindex="-1"
          class="tab-btn-standard group relative flex-1 min-w-0 border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 whitespace-nowrap border-b-2 py-3 px-2 sm:px-3 text-center text-xs sm:text-sm font-medium transition-colors"
        >
          <svg class="mx-auto h-4 w-4 sm:h-5 sm:w-5 mb-0.5 group-hover:text-indigo-500 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 3.055A9.001 9.001 0 1020.945 13H11V3.055z"></path>
          </svg>
          Stats
        </button>
        <button
          type="button"
          onclick="switchTab('media', 'standard')"
          id="tab-standard-media"
          role="tab"
          aria-controls="content-media"
          aria-selected="false"
          tabindex="-1"
          class="tab-btn-standard group relative flex-1 min-w-0 border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 whitespace-nowrap border-b-2 py-3 px-2 sm:px-3 text-center text-xs sm:text-sm font-medium transition-colors"
        >
          <svg class="mx-auto h-4 w-4 sm:h-5 sm:w-5 mb-0.5 group-hover:text-indigo-500 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 10l4.553-2.276A1 1 0 0121 8.618v6.764a1 1 0 01-1.447.894L15 14M5 18h8a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v8a2 2 0 002 2z"></path>
          </svg>
          Media
        </button>
        <button
          type="button"
          onclick="switchTab('quickactions', 'standard')"
          id="tab-standard-quickactions"
          role="tab"
          aria-controls="content-quickactions"
          aria-selected="false"
          tabindex="-1"
          class="tab-btn-standard group relative flex-1 min-w-0 border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 whitespace-nowrap border-b-2 py-3 px-2 sm:px-3 text-center text-xs sm:text-sm font-medium transition-colors"
        >
          <svg class="mx-auto h-4 w-4 sm:h-5 sm:w-5 mb-0.5 group-hover:text-indigo-500 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
          </svg>
          Actions
        </button>
      </nav>
      </div>

      <!-- Premium Tabs Navigation -->
      <div data-tier="premium" class="hidden w-full overflow-x-auto scrollbar-hide">
      <nav class="-mb-px flex w-full flex-wrap sm:flex-nowrap" aria-label="Premium Tabs" role="tablist">
        <button
          type="button"
          onclick="switchTab('summary', 'premium')"
          id="tab-premium-summary"
          role="tab"
          aria-controls="content-summary"
          aria-selected="true"
          tabindex="0"
          class="tab-btn-premium group relative flex-1 min-w-0 border-indigo-500 text-indigo-600 whitespace-nowrap border-b-2 py-3 px-2 sm:px-3 text-center text-xs sm:text-sm font-medium transition-colors"
        >
          <svg class="mx-auto h-4 w-4 sm:h-5 sm:w-5 mb-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"></path>
          </svg>
          Summary
        </button>
        <button
          type="button"
          onclick="switchTab('topics', 'premium')"
          id="tab-premium-topics"
          role="tab"
          aria-controls="content-topics"
          aria-selected="false"
          tabindex="-1"
          class="tab-btn-premium group relative flex-1 min-w-0 border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 whitespace-nowrap border-b-2 py-3 px-2 sm:px-3 text-center text-xs sm:text-sm font-medium transition-colors"
        >
          <svg class="mx-auto h-4 w-4 sm:h-5 sm:w-5 mb-0.5 group-hover:text-indigo-500 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 10h16M4 14h10"></path>
          </svg>
          Topics
        </button>
        <button
          type="button"
          onclick="switchTab('highlights', 'premium')"
          id="tab-premium-highlights"
          role="tab"
          aria-controls="content-highlights"
          aria-selected="false"
          tabindex="-1"
          class="tab-btn-premium group relative flex-1 min-w-0 border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 whitespace-nowrap border-b-2 py-3 px-2 sm:px-3 text-center text-xs sm:text-sm font-medium transition-colors"
        >
          <svg class="mx-auto h-5 w-5 mb-0.5 group-hover:text-indigo-500 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
          </svg>
          Highlights
        </button>
        <button
          type="button"
          onclick="switchTab('detailed', 'premium')"
          id="tab-premium-detailed"
          role="tab"
          aria-controls="content-detailed"
          aria-selected="false"
          tabindex="-1"
          class="tab-btn-premium group relative flex-1 min-w-0 border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 whitespace-nowrap border-b-2 py-3 px-2 sm:px-3 text-center text-xs sm:text-sm font-medium transition-colors"
        >
          <svg class="mx-auto h-5 w-5 mb-0.5 group-hover:text-indigo-500 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 10h.01M12 10h.01M16 10h.01M9 16H5a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v8a2 2 0 01-2 2h-5l-4 4v-4z"></path>
          </svg>
          <span class="hidden sm:inline">Detailed Notes</span>
          <span class="sm:hidden">Notes</span>
        </button>
        <button
          type="button"
          onclick="switchTab('actions', 'premium')"
          id="tab-premium-actions"
          role="tab"
          aria-controls="content-actions"
          aria-selected="false"
          tabindex="-1"
          class="tab-btn-premium group relative flex-1 min-w-0 border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 whitespace-nowrap border-b-2 py-3 px-2 sm:px-3 text-center text-xs sm:text-sm font-medium transition-colors"
        >
          <svg class="mx-auto h-5 w-5 mb-0.5 group-hover:text-indigo-500 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-6 9l2 2 4-4"></path>
          </svg>
          Actions
          <% if (meeting.superAgentAnalysis?.actionItems && meeting.superAgentAnalysis.actionItems.length > 0) { %>
            <span class="absolute -top-1 -right-1 inline-flex items-center justify-center h-5 w-5 rounded-full bg-amber-500 text-xs font-bold text-white"><%= meeting.superAgentAnalysis.actionItems.length %></span>
          <% } %>
        </button>
        <button
          type="button"
          onclick="switchTab('followups', 'premium')"
          id="tab-premium-followups"
          role="tab"
          aria-controls="content-followups"
          aria-selected="false"
          tabindex="-1"
          class="tab-btn-premium group relative flex-1 min-w-0 border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 whitespace-nowrap border-b-2 py-3 px-2 sm:px-3 text-center text-xs sm:text-sm font-medium transition-colors"
        >
          <svg class="mx-auto h-5 w-5 mb-0.5 group-hover:text-indigo-500 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 9l3 3m0 0l-3 3m3-3H8m13 0a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
          <span class="hidden sm:inline">Follow-ups</span>
          <span class="sm:hidden">Follow</span>
        </button>
        <button
          type="button"
          onclick="switchTab('transcript', 'premium')"
          id="tab-premium-transcript"
          role="tab"
          aria-controls="content-transcript"
          aria-selected="false"
          tabindex="-1"
          class="tab-btn-premium group relative flex-1 min-w-0 border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 whitespace-nowrap border-b-2 py-3 px-2 sm:px-3 text-center text-xs sm:text-sm font-medium transition-colors"
        >
          <svg class="mx-auto h-5 w-5 mb-0.5 group-hover:text-indigo-500 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
          </svg>
          Transcript
        </button>
        <button
          type="button"
          onclick="switchTab('stats', 'premium')"
          id="tab-premium-stats"
          role="tab"
          aria-controls="content-stats"
          aria-selected="false"
          tabindex="-1"
          class="tab-btn-premium group relative flex-1 min-w-0 border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 whitespace-nowrap border-b-2 py-3 px-2 sm:px-3 text-center text-xs sm:text-sm font-medium transition-colors"
        >
          <svg class="mx-auto h-4 w-4 sm:h-5 sm:w-5 mb-0.5 group-hover:text-indigo-500 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 3.055A9.001 9.001 0 1020.945 13H11V3.055z"></path>
          </svg>
          Stats
        </button>
        <button
          type="button"
          onclick="switchTab('media', 'premium')"
          id="tab-premium-media"
          role="tab"
          aria-controls="content-media"
          aria-selected="false"
          tabindex="-1"
          class="tab-btn-premium group relative flex-1 min-w-0 border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 whitespace-nowrap border-b-2 py-3 px-2 sm:px-3 text-center text-xs sm:text-sm font-medium transition-colors"
        >
          <svg class="mx-auto h-4 w-4 sm:h-5 sm:w-5 mb-0.5 group-hover:text-indigo-500 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 10l4.553-2.276A1 1 0 0121 8.618v6.764a1 1 0 01-1.447.894L15 14M5 18h8a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v8a2 2 0 002 2z"></path>
          </svg>
          Media
        </button>
        <button
          type="button"
          onclick="switchTab('quickactions', 'premium')"
          id="tab-premium-quickactions"
          role="tab"
          aria-controls="content-quickactions"
          aria-selected="false"
          tabindex="-1"
          class="tab-btn-premium group relative flex-1 min-w-0 border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 whitespace-nowrap border-b-2 py-3 px-2 sm:px-3 text-center text-xs sm:text-sm font-medium transition-colors"
        >
          <svg class="mx-auto h-4 w-4 sm:h-5 sm:w-5 mb-0.5 group-hover:text-indigo-500 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
          </svg>
          Actions
        </button>
      </nav>
      </div>
    </div>
  </div>

  <div class="px-4 py-5 sm:px-6">
    <div id="content-summary" class="tab-content" role="tabpanel" aria-labelledby="tab-summary">
      <%- include('tab-summary', { meeting, publishDeliveries }) -%>
    </div>


    <!-- Topics Tab -->
    <div id="content-topics" class="tab-content hidden" role="tabpanel" aria-labelledby="tab-topics">
      <div data-tier="standard">
      <% if (meeting.topics && meeting.topics.length > 0) { %>
        <% 
          function topicTitle(topic, idx) {
            if (typeof topic === 'string') return topic || `Topic ${idx + 1}`;
            if (Array.isArray(topic)) return `Topic ${idx + 1}`;
            return topic.title || topic.name || topic.topic || topic.heading || topic.label || `Topic ${idx + 1}`;
          }
          function topicBullets(topic) {
            if (typeof topic === 'string') return [topic];
            if (Array.isArray(topic)) return topic.map(item => typeof item === 'string' ? item : (item.text || item.summary || item.description || item.details || JSON.stringify(item)));
            const arrayKeys = ['items', 'points', 'bullets', 'notes', 'entries', 'highlights', 'list'];
            for (const key of arrayKeys) {
              if (Array.isArray(topic[key]) && topic[key].length > 0) {
                return topic[key].map(item => typeof item === 'string' ? item : (item.text || item.summary || item.description || item.details || item.content || item.note || JSON.stringify(item)));
              }
            }
            if (topic.details && typeof topic.details === 'string') return [topic.details];
            if (topic.summary) return [topic.summary];
            if (topic.description) return [topic.description];
            if (topic.content) return [topic.content];
            if (topic.text) return [topic.text];
            const keys = Object.keys(topic).filter(k => !['topic', 'title', 'name', 'heading', 'label', 'id', 'index'].includes(k));
            if (keys.length === 0) return ['No details available'];
            return [JSON.stringify(topic)];
          }
          function parseTimeFromText(txt) {
            if (!txt || typeof txt !== 'string') return null;
            const match = txt.match(/(\d{1,2}:)?\d{1,2}:\d{2}/);
            if (!match) return null;
            const parts = match[0].split(':').map(Number);
            if (parts.length === 3) {
              return parts[0] * 3600 + parts[1] * 60 + parts[2];
            }
            return parts[0] * 60 + parts[1];
          }
          function formatTsLabel(sec) {
            const hours = Math.floor(sec / 3600);
            const minutes = Math.floor((sec % 3600) / 60);
            const seconds = Math.floor(sec % 60);
            if (hours > 0) return `${hours}:${String(minutes).padStart(2, '0')}:${String(seconds).padStart(2, '0')}`;
            return `${minutes}:${String(seconds).padStart(2, '0')}`;
          }
        %>
        <div class="flex items-center justify-between mb-4">
          <div>
            <h3 class="text-base font-semibold text-gray-900">Topics</h3>
            <p class="text-sm text-gray-500">Expand topics, jump to timestamps, or copy.</p>
          </div>
          <div class="flex items-center gap-2">
            <button 
              type="button" 
              onclick="copyAllTopics()"
              class="inline-flex items-center rounded-md bg-indigo-50 px-3 py-2 text-sm font-medium text-indigo-700 ring-1 ring-inset ring-indigo-200 hover:bg-indigo-100"
              title="Copy entire recap"
            >
              <svg class="h-4 w-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 16H6a2 2 0 01-2-2V6a2 2 0 012-2h8a2 2 0 012 2v2m-6 12h8a2 2 0 002-2v-8a2 2 0 00-2-2h-8a2 2 0 00-2 2v8a2 2 0 002 2z"></path>
              </svg>
              Copy recap
            </button>
          </div>
        </div>
        <div class="divide-y divide-gray-200 border border-gray-200 rounded-lg">
          <% meeting.topics.forEach((topic, idx) => { 
            const title = topicTitle(topic, idx);
            const bullets = topicBullets(topic);
          %>
            <div class="topic-accordion">
              <div class="w-full flex items-center justify-between px-4 py-3 hover:bg-gray-50">
                <button 
                  type="button" 
                  class="flex-1 flex items-center gap-2 text-left"
                  onclick="toggleTopicAccordion(<%= idx %>)"
                  aria-expanded="<%= idx === 0 ? 'true' : 'false' %>"
                  aria-controls="topic-panel-<%= idx %>"
                >
                  <svg id="topic-chevron-<%= idx %>" class="h-4 w-4 text-gray-500 transform transition-transform <%= idx === 0 ? 'rotate-90' : 'rotate-0' %>" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                  </svg>
                  <span class="text-sm font-semibold text-gray-900"><%= title %></span>
                </button>
                <button 
                  type="button" 
                  onclick="copyTopic(<%= idx %>, event)"
                  class="inline-flex items-center rounded-md px-2 py-1 text-xs font-medium text-gray-600 ring-1 ring-inset ring-gray-200 hover:bg-gray-100"
                  title="Copy topic"
                >
                  <svg class="h-3.5 w-3.5 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 16H6a2 2 0 01-2-2V6a2 2 0 012-2h8a2 2 0 012 2v2m-6 12h8a2 2 0 002-2v-8a2 2 0 00-2-2h-8a2 2 0 00-2 2v8a2 2 0 002 2z"></path>
                  </svg>
                  Copy
                </button>
              </div>
              <div id="topic-panel-<%= idx %>" class="<%= idx === 0 ? '' : 'hidden' %> border-t border-gray-100 bg-white" role="region">
                <ul class="px-4 py-3 space-y-3">
                  <% bullets.forEach((bullet) => { 
                    const text = typeof bullet === 'object' ? (bullet.text || bullet.summary || bullet.content || bullet.description || bullet.note || JSON.stringify(bullet)) : bullet;
                    const numericTs = typeof bullet === 'object' ? Number(bullet.timestamp_seconds ?? bullet.timestamp ?? bullet.ts ?? bullet.time ?? bullet.time_seconds ?? (bullet.startTimeMs ? bullet.startTimeMs / 1000 : NaN)) : NaN;
                    const parsedTs = Number.isNaN(numericTs) ? parseTimeFromText(text) : numericTs;
                    const tsSeconds = Number.isNaN(parsedTs) ? null : Math.max(0, Math.floor(parsedTs));
                    const tsLabel = tsSeconds !== null ? formatTsLabel(tsSeconds) : null;
                  %>
                    <li class="flex items-start gap-3">
                      <span class="mt-1 h-1.5 w-1.5 rounded-full bg-indigo-400 flex-shrink-0"></span>
                      <div class="flex-1">
                        <p class="text-sm text-gray-800"><%= text %></p>
                        <div class="mt-1 flex flex-wrap gap-2 text-xs text-gray-500">
                          <% if (tsSeconds !== null) { %>
                            <button 
                              type="button" 
                              class="inline-flex items-center rounded-full bg-indigo-50 px-2 py-0.5 text-indigo-700 ring-1 ring-indigo-200 hover:bg-indigo-100"
                              onclick="jumpToTimestamp(<%= tsSeconds %>)"
                            >
                              <svg class="h-3 w-3 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                              </svg>
                              <%= tsLabel %>
                            </button>
                          <% } %>
                        </div>
                      </div>
                    </li>
                  <% }); %>
                </ul>
              </div>
            </div>
          <% }); %>
        </div>
      <% } else { %>
        <div class="text-center py-8">
          <svg class="mx-auto h-12 w-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 10h.01M12 10h.01M16 10h.01M9 16H5a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v8a2 2 0 00-2 2h-5l-4 4v-4z"></path>
          </svg>
          <h3 class="mt-2 text-sm font-semibold text-gray-900">No topics yet</h3>
          <p class="mt-1 text-sm text-gray-500">Topics will appear here once the meeting summary is generated.</p>
        </div>
      <% } %>
      </div>

      <div data-tier="premium" class="hidden">
        <% if (!meeting.hasPremiumAccess) { %>
          <%- include('premium-cta', { meeting }) -%>
        <% } else if (!meeting.superAgentAnalysis || meeting.superAgentAnalysis.status !== 'completed') { %>
          <div class="text-center py-8">
            <svg class="mx-auto h-12 w-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6l4 2m6-2a10 10 0 11-20 0 10 10 0 0120 0z"></path>
            </svg>
            <h3 class="mt-2 text-sm font-semibold text-gray-900">Premium chapters not ready</h3>
            <p class="mt-1 text-sm text-gray-500">Run Super Agent to generate AssemblyAI chapters.</p>
            <% if (meeting.artifactId) { %>
              <button
                type="button"
                onclick="openSuperAgentPanel()"
                class="mt-4 inline-flex items-center rounded-md bg-amber-500 px-3 py-2 text-sm font-semibold text-white shadow-sm hover:bg-amber-400"
              >
                Analyse with Super Agent
              </button>
            <% } %>
          </div>
        <% } else { %>
          <% const chapters = meeting.superAgentAnalysis?.chapters || []; %>
          <% if (chapters.length > 0) { %>
            <div class="flex items-center justify-between mb-4">
              <div>
                <h3 class="text-base font-semibold text-gray-900">Chapters</h3>
                <p class="text-sm text-gray-500">AssemblyAI chapters with timestamps.</p>
              </div>
            </div>
            <div class="divide-y divide-gray-200 border border-gray-200 rounded-lg">
              <% chapters.forEach((chapter, idx) => { 
                const start = typeof chapter.start === 'number' ? Math.floor(chapter.start / 1000) : null;
                const end = typeof chapter.end === 'number' ? Math.floor(chapter.end / 1000) : null;
                const title = chapter.headline || chapter.gist || `Chapter ${idx + 1}`;
              %>
                <div class="px-4 py-3">
                  <div class="flex items-center justify-between">
                    <p class="text-sm font-semibold text-gray-900"><%= title %></p>
                    <% if (start !== null) { %>
                      <button 
                        type="button" 
                        class="inline-flex items-center rounded-full bg-indigo-50 px-2 py-0.5 text-xs text-indigo-700 ring-1 ring-indigo-200 hover:bg-indigo-100"
                        onclick="jumpToTimestamp(<%= start %>)"
                      >
                        <svg class="h-3 w-3 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                        </svg>
                        <%= Math.floor(start / 60) %>:<%= String(Math.floor(start % 60)).padStart(2, '0') %><%= end !== null ? `-${Math.floor(end / 60)}:${String(Math.floor(end % 60)).padStart(2, '0')}` : '' %>
                      </button>
                    <% } %>
                  </div>
                  <% if (chapter.summary) { %>
                    <p class="mt-2 text-sm text-gray-700"><%= chapter.summary %></p>
                  <% } %>
                </div>
              <% }); %>
            </div>
          <% } else { %>
            <div class="text-center py-8">
              <svg class="mx-auto h-12 w-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 10h.01M12 10h.01M16 10h.01M9 16H5a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v8a2 2 0 00-2 2h-5l-4 4v-4z"></path>
              </svg>
              <h3 class="mt-2 text-sm font-semibold text-gray-900">No chapters available</h3>
              <p class="mt-1 text-sm text-gray-500">AssemblyAI did not return chapters for this meeting.</p>
            </div>
          <% } %>
        <% } %>
      </div>
    </div>

    <!-- Highlights Tab -->
    <div id="content-highlights" class="tab-content hidden" role="tabpanel" aria-labelledby="tab-highlights">
      <div data-tier="standard">
      <% if (meeting.highlights && meeting.highlights.length > 0) { %>
        <ul class="space-y-3">
          <% meeting.highlights.forEach(function(item, idx) { %>
            <li class="p-3 rounded-lg border border-gray-100 bg-gray-50">
              <div class="flex items-start justify-between">
                <div>
                  <p class="text-sm font-semibold text-gray-900"><%= item.title || 'Highlight ' + (idx + 1) %></p>
                  <p class="mt-1 text-sm text-gray-700"><%= item.summary || item.text || '' %></p>
                  <div class="mt-2 flex flex-wrap gap-2 text-xs text-gray-500">
                    <% if (item.speaker) { %>
                      <span class="inline-flex items-center rounded-full bg-white px-2 py-0.5 ring-1 ring-gray-200">
                        <svg class="h-3 w-3 mr-1 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                        </svg>
                        <%= item.speaker %>
                      </span>
                    <% } %>
                    <% const highlightTs = Number(item.timestamp_seconds); if (!Number.isNaN(highlightTs)) { %>
                      <button 
                        type="button" 
                        class="inline-flex items-center rounded-full bg-indigo-50 px-2 py-0.5 text-indigo-700 ring-1 ring-indigo-200 hover:bg-indigo-100 cursor-pointer transition-colors"
                        onclick="jumpToTimestamp(<%= Math.floor(highlightTs) %>)"
                      >
                        <svg class="h-3 w-3 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                        </svg>
                        <%= Math.floor(highlightTs / 60) %>:<%= String(Math.floor(highlightTs % 60)).padStart(2, '0') %>
                      </button>
                    <% } %>
                    <% if (item.category) { %>
                      <span class="inline-flex items-center rounded-full bg-purple-50 px-2 py-0.5 text-purple-700 ring-1 ring-purple-200 capitalize"><%= item.category %></span>
                    <% } %>
                    <% if (item.impact) { %>
                      <span class="inline-flex items-center rounded-full bg-amber-50 px-2 py-0.5 text-amber-700 ring-1 ring-amber-200 capitalize">Impact: <%= item.impact %></span>
                    <% } %>
                  </div>
                </div>
                <span class="text-xs text-gray-400 ml-2">#<%= idx + 1 %></span>
              </div>
            </li>
          <% }); %>
        </ul>
      <% } else { %>
        <div class="text-center py-8">
          <svg class="mx-auto h-12 w-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
          </svg>
          <h3 class="mt-2 text-sm font-semibold text-gray-900">No highlights yet</h3>
          <p class="mt-1 text-sm text-gray-500">Key decisions and outcomes will appear here after processing.</p>
        </div>
      <% } %>
      </div>

      <div data-tier="premium" class="hidden">
        <% if (!meeting.hasPremiumAccess) { %>
          <%- include('premium-cta', { meeting }) -%>
        <% } else if (!meeting.superAgentAnalysis || meeting.superAgentAnalysis.status !== 'completed') { %>
          <div class="text-center py-8">
            <svg class="mx-auto h-12 w-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
            </svg>
            <h3 class="mt-2 text-sm font-semibold text-gray-900">Premium highlights not ready</h3>
            <p class="mt-1 text-sm text-gray-500">Run Super Agent to generate AssemblyAI highlights.</p>
            <% if (meeting.artifactId) { %>
              <button
                type="button"
                onclick="openSuperAgentPanel()"
                class="mt-4 inline-flex items-center rounded-md bg-amber-500 px-3 py-2 text-sm font-semibold text-white shadow-sm hover:bg-amber-400"
              >
                Analyse with Super Agent
              </button>
            <% } %>
          </div>
        <% } else { %>
          <% const premiumHighlights = meeting.superAgentAnalysis?.highlights || []; %>
          <% if (premiumHighlights.length > 0) { %>
            <ul class="space-y-3">
              <% premiumHighlights.forEach(function(item, idx) { %>
                <li class="p-3 rounded-lg border border-gray-100 bg-gray-50">
                  <div class="flex items-start justify-between">
                    <div>
                      <p class="text-sm font-semibold text-gray-900"><%= item.title || 'Highlight ' + (idx + 1) %></p>
                      <p class="mt-1 text-sm text-gray-700"><%= item.summary || item.text || '' %></p>
                      <div class="mt-2 flex flex-wrap gap-2 text-xs text-gray-500">
                        <% if (item.speaker) { %>
                          <span class="inline-flex items-center rounded-full bg-white px-2 py-0.5 ring-1 ring-gray-200">
                            <svg class="h-3 w-3 mr-1 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                            </svg>
                            <%= item.speaker %>
                          </span>
                        <% } %>
                        <% const highlightTs = Number(item.timestamp_seconds); if (!Number.isNaN(highlightTs)) { %>
                          <button 
                            type="button" 
                            class="inline-flex items-center rounded-full bg-indigo-50 px-2 py-0.5 text-indigo-700 ring-1 ring-indigo-200 hover:bg-indigo-100 cursor-pointer transition-colors"
                            onclick="jumpToTimestamp(<%= Math.floor(highlightTs) %>)"
                          >
                            <svg class="h-3 w-3 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                            </svg>
                            <%= Math.floor(highlightTs / 60) %>:<%= String(Math.floor(highlightTs % 60)).padStart(2, '0') %>
                          </button>
                        <% } %>
                        <% if (item.category) { %>
                          <span class="inline-flex items-center rounded-full bg-purple-50 px-2 py-0.5 text-purple-700 ring-1 ring-purple-200 capitalize"><%= item.category %></span>
                        <% } %>
                        <% if (item.impact) { %>
                          <span class="inline-flex items-center rounded-full bg-amber-50 px-2 py-0.5 text-amber-700 ring-1 ring-amber-200 capitalize">Impact: <%= item.impact %></span>
                        <% } %>
                      </div>
                    </div>
                    <span class="text-xs text-gray-400 ml-2">#<%= idx + 1 %></span>
                  </div>
                </li>
              <% }); %>
            </ul>
          <% } else { %>
            <div class="text-center py-8">
              <svg class="mx-auto h-12 w-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
              </svg>
              <h3 class="mt-2 text-sm font-semibold text-gray-900">No highlights yet</h3>
              <p class="mt-1 text-sm text-gray-500">AssemblyAI highlights will appear here after processing.</p>
            </div>
          <% } %>
        <% } %>
      </div>
    </div>

    <!-- Detailed Notes Tab -->
    <div id="content-detailed" class="tab-content hidden" role="tabpanel" aria-labelledby="tab-detailed">
      <div data-tier="standard">
      <% if (meeting.detailedNotes && meeting.detailedNotes.length > 0) { %>
        <div class="space-y-3">
          <% meeting.detailedNotes.forEach(function(item, idx) { %>
            <div class="p-3 rounded-lg border border-gray-100 bg-white shadow-sm">
              <div class="flex items-start justify-between">
                <div class="flex items-center gap-2 text-xs text-gray-500">
                  <span class="inline-flex items-center rounded-full bg-gray-100 px-2 py-0.5 text-gray-700">
                    <svg class="h-3 w-3 mr-1 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                    </svg>
                    <%= item.speaker || 'Speaker' %>
                  </span>
                  <% const detailTs = Number(item.timestamp_seconds); if (!Number.isNaN(detailTs)) { %>
                    <button 
                      type="button" 
                      class="inline-flex items-center rounded-full bg-indigo-50 px-2 py-0.5 text-indigo-700 ring-1 ring-indigo-200 hover:bg-indigo-100 cursor-pointer transition-colors"
                      onclick="jumpToTimestamp(<%= Math.floor(detailTs) %>)"
                    >
                      <svg class="h-3 w-3 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                      </svg>
                      <%= Math.floor(detailTs / 60) %>:<%= String(Math.floor(detailTs % 60)).padStart(2, '0') %>
                    </button>
                  <% } %>
                  <% if (item.topic) { %>
                    <span class="inline-flex items-center rounded-full bg-blue-50 px-2 py-0.5 text-blue-700 ring-1 ring-blue-200"><%= item.topic %></span>
                  <% } %>
                  <% if (item.importance) { %>
                    <span class="inline-flex items-center rounded-full bg-amber-50 px-2 py-0.5 text-amber-700 ring-1 ring-amber-200 capitalize">Priority: <%= item.importance %></span>
                  <% } %>
                </div>
                <span class="text-xs text-gray-400 ml-2">#<%= idx + 1 %></span>
              </div>
              <p class="mt-2 text-sm text-gray-800 font-medium"><%= item.paraphrase || item.summary || item.text || '' %></p>
              <% if (item.quote) { %>
                <p class="mt-1 text-sm text-gray-500 italic">&ldquo;<%= item.quote %>&rdquo;</p>
              <% } %>
            </div>
          <% }); %>
        </div>
      <% } else { %>
        <div class="text-center py-8">
          <svg class="mx-auto h-12 w-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 10h.01M12 10h.01M16 10h.01M9 16H5a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v8a2 2 0 01-2 2h-5l-4 4v-4z"></path>
          </svg>
          <h3 class="mt-2 text-sm font-semibold text-gray-900">No detailed notes yet</h3>
          <p class="mt-1 text-sm text-gray-500">Paraphrased key phrases will appear here for richer context.</p>
        </div>
      <% } %>
      </div>

      <div data-tier="premium" class="hidden">
        <% if (!meeting.hasPremiumAccess) { %>
          <%- include('premium-cta', { meeting }) -%>
        <% } else { %>
          <div class="text-center py-8">
            <svg class="mx-auto h-12 w-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 10h.01M12 10h.01M16 10h.01M9 16H5a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v8a2 2 0 00-2 2h-5l-4 4v-4z"></path>
            </svg>
            <h3 class="mt-2 text-sm font-semibold text-gray-900">Detailed notes not available</h3>
            <p class="mt-1 text-sm text-gray-500">AssemblyAI Super Agent does not return detailed notes yet.</p>
          </div>
        <% } %>
      </div>
    </div>

    <!-- Actions Tab -->
    <div id="content-actions" class="tab-content hidden" role="tabpanel" aria-labelledby="tab-actions">
      <div data-tier="standard">
      <% if (meeting.actionItems && meeting.actionItems.length > 0) { %>
        <%- include('action-columns', { actions: meeting.actionItems }) -%>
      <% } else { %>
        <div class="text-center py-8">
          <svg class="mx-auto h-12 w-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-6 9l2 2 4-4"></path>
          </svg>
          <h3 class="mt-2 text-sm font-semibold text-gray-900">No action items</h3>
          <p class="mt-1 text-sm text-gray-500">Action items extracted from the meeting will appear here.</p>
        </div>
      <% } %>
      </div>

      <div data-tier="premium" class="hidden">
        <% if (!meeting.hasPremiumAccess) { %>
          <%- include('premium-cta', { meeting }) -%>
        <% } else if (!meeting.superAgentAnalysis || meeting.superAgentAnalysis.status !== 'completed') { %>
          <div class="text-center py-8">
            <svg class="mx-auto h-12 w-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-6 9l2 2 4-4"></path>
            </svg>
            <h3 class="mt-2 text-sm font-semibold text-gray-900">Premium action items not ready</h3>
            <p class="mt-1 text-sm text-gray-500">Run Super Agent to generate timestamped actions.</p>
            <% if (meeting.artifactId) { %>
              <button
                type="button"
                onclick="openSuperAgentPanel()"
                class="mt-4 inline-flex items-center rounded-md bg-amber-500 px-3 py-2 text-sm font-semibold text-white shadow-sm hover:bg-amber-400"
              >
                Analyse with Super Agent
              </button>
            <% } %>
          </div>
        <% } else { %>
          <% const premiumActions = meeting.superAgentAnalysis?.actionItems || []; %>
          <% if (premiumActions.length > 0) { %>
            <%- include('action-columns', { actions: premiumActions }) -%>
          <% } else { %>
            <div class="text-center py-8">
              <svg class="mx-auto h-12 w-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-6 9l2 2 4-4"></path>
              </svg>
              <h3 class="mt-2 text-sm font-semibold text-gray-900">No action items</h3>
              <p class="mt-1 text-sm text-gray-500">AssemblyAI action items will appear here after processing.</p>
            </div>
          <% } %>
        <% } %>
      </div>
    </div>

    <!-- Follow-ups Tab -->
    <div id="content-followups" class="tab-content hidden" role="tabpanel" aria-labelledby="tab-followups">
      <div data-tier="standard">
      <% if (meeting.followUps && meeting.followUps.length > 0) { %>
        <ul class="space-y-2">
          <% meeting.followUps.forEach(function(item) { %>
            <li class="flex items-start bg-blue-50 rounded-lg p-3">
              <svg class="h-5 w-5 text-blue-500 mt-0.5 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-8.707l-3-3a1 1 0 00-1.414 1.414L10.586 9H7a1 1 0 100 2h3.586l-1.293 1.293a1 1 0 101.414 1.414l3-3a1 1 0 000-1.414z" clip-rule="evenodd"></path>
              </svg>
              <span class="ml-3 text-sm text-gray-700">
                <% if (typeof item === 'object') { %>
                  <%= item.description || item.text || item.task || JSON.stringify(item) %>
                <% } else { %>
                  <%= item %>
                <% } %>
              </span>
            </li>
          <% }); %>
        </ul>
      <% } else { %>
        <div class="text-center py-8">
          <svg class="mx-auto h-12 w-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 9l3 3m0 0l-3 3m3-3H8m13 0a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
          <h3 class="mt-2 text-sm font-semibold text-gray-900">No follow-ups</h3>
          <p class="mt-1 text-sm text-gray-500">Suggested follow-ups from the meeting will appear here.</p>
        </div>
      <% } %>
      </div>

      <div data-tier="premium" class="hidden">
        <% if (!meeting.hasPremiumAccess) { %>
          <%- include('premium-cta', { meeting }) -%>
        <% } else { %>
          <div class="text-center py-8">
            <svg class="mx-auto h-12 w-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 9l3 3m0 0l-3 3m3-3H8m13 0a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
            <h3 class="mt-2 text-sm font-semibold text-gray-900">Follow-ups not available</h3>
            <p class="mt-1 text-sm text-gray-500">AssemblyAI Super Agent does not return follow-ups yet.</p>
          </div>
        <% } %>
      </div>
    </div>

    <!-- Transcript Tab - Lazy Loaded -->
    <div id="content-transcript" class="tab-content hidden w-full" role="tabpanel" aria-labelledby="tab-transcript" data-lazy-load="true" data-loaded="false">
      <!-- Loading State -->
      <div id="transcript-loading" class="<%= meeting.lazyLoadTranscript ? '' : 'hidden' %>">
        <div class="flex items-center justify-center py-8">
          <div class="text-center">
            <svg class="animate-spin h-10 w-10 text-indigo-600 mx-auto mb-4" fill="none" viewBox="0 0 24 24">
              <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
              <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
            </svg>
            <h3 class="text-sm font-semibold text-gray-900">Loading transcript...</h3>
            <p class="mt-1 text-sm text-gray-500">
              <% if (meeting.transcriptChunkCount) { %>
                Fetching <%= meeting.transcriptChunkCount.toLocaleString() %> transcript segments
              <% } else { %>
                Please wait while we load the transcript
              <% } %>
            </p>
          </div>
        </div>
        <!-- Skeleton placeholders -->
        <div class="space-y-4 animate-pulse px-4 sm:px-6 lg:px-8">
          <% for (let i = 0; i < 5; i++) { %>
            <div class="pt-3 border-t border-gray-100">
              <div class="flex items-center mb-1">
                <div class="h-5 w-24 bg-gray-200 rounded-full"></div>
                <div class="ml-2 h-4 w-12 bg-gray-100 rounded"></div>
              </div>
              <div class="space-y-2">
                <div class="h-4 bg-gray-200 rounded w-full"></div>
                <div class="h-4 bg-gray-200 rounded w-5/6"></div>
                <div class="h-4 bg-gray-200 rounded w-4/6"></div>
              </div>
            </div>
          <% } %>
        </div>
      </div>
      
      <!-- Transcript Content (populated by JavaScript or server) -->
      <div id="transcript-content" class="w-full <%= meeting.lazyLoadTranscript ? 'hidden' : '' %>">
        <% if (!meeting.lazyLoadTranscript) { %>
          <% if (meeting.transcript && meeting.transcript.length > 0) { %>
            <% 
              const speakerPalette = ['indigo','blue','purple','emerald','amber','rose'];
              const speakerClass = (name) => {
                const str = name || 'Speaker';
                let hash = 0;
                for (let i = 0; i < str.length; i++) { hash = (hash << 5) - hash + str.charCodeAt(i); hash |= 0; }
                const idx = Math.abs(hash) % speakerPalette.length;
                return speakerPalette[idx];
              };
            %>
            <div class="max-h-[calc(100vh-300px)] min-h-[400px] overflow-y-auto w-full">
              <div class="w-full space-y-4 pb-4 px-4 sm:px-6 lg:px-8">
                <% 
                  let currentSpeaker = null;
                  meeting.transcript.forEach(function(chunk) { 
                    const showSpeaker = chunk.speaker !== currentSpeaker;
                    currentSpeaker = chunk.speaker;
                %>
                  <div class="w-full <%= showSpeaker ? 'pt-3 border-t border-gray-100' : '' %>">
                    <% if (showSpeaker) { %>
                      <div class="flex items-center mb-1">
                        <% const color = speakerClass(chunk.speaker); %>
                        <span class="inline-flex items-center rounded-full px-2.5 py-0.5 text-xs font-medium bg-<%= color %>-100 text-<%= color %>-800">
                          <svg class="mr-1 h-3 w-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                          </svg>
                          <%= chunk.speaker %>
                        </span>
                        <% if (chunk.startTimeMs) { %>
                          <span class="ml-2 text-xs text-gray-400">
                            <%= Math.floor(chunk.startTimeMs / 60000) %>:<%= String(Math.floor((chunk.startTimeMs % 60000) / 1000)).padStart(2, '0') %>
                          </span>
                        <% } %>
                      </div>
                    <% } %>
                    <p class="w-full text-sm text-gray-700 text-left">
                      <%= chunk.text %>
                    </p>
                  </div>
                <% }); %>
                <div class="pt-4 border-t border-gray-100 text-center">
                  <p class="text-xs text-gray-400"><%= meeting.transcript.length.toLocaleString() %> transcript segments loaded</p>
                </div>
              </div>
            </div>
          <% } else { %>
            <div class="text-center py-8">
              <svg class="mx-auto h-12 w-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
              </svg>
              <h3 class="mt-2 text-sm font-semibold text-gray-900">No transcript available</h3>
              <p class="mt-1 text-sm text-gray-500">The meeting transcript will appear here once it's processed.</p>
            </div>
          <% } %>
        <% } %>
      </div>
      
      <!-- Error State -->
      <div id="transcript-error" class="hidden text-center py-8">
        <svg class="mx-auto h-12 w-12 text-red-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path>
        </svg>
        <h3 class="mt-2 text-sm font-semibold text-gray-900">Failed to load transcript</h3>
        <p class="mt-1 text-sm text-gray-500" id="transcript-error-message">An error occurred while loading the transcript.</p>
        <button onclick="loadTranscript()" class="mt-4 inline-flex items-center px-3 py-2 border border-gray-300 shadow-sm text-sm leading-4 font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">
          <svg class="mr-2 h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"></path>
          </svg>
          Retry
        </button>
      </div>
    </div>

    <!-- Stats Tab -->
    <div id="content-stats" class="tab-content hidden" role="tabpanel" aria-labelledby="tab-stats">
      <% const stats = meeting.stats; %>
      <% if (stats && ((stats.speakers && stats.speakers.length) || stats.durationSeconds)) { %>
        <div class="space-y-4">
          <% if (stats.durationSeconds) { %>
            <div class="rounded-lg border border-gray-100 bg-gray-50 p-4 flex flex-col sm:flex-row sm:items-center sm:justify-between gap-3">
              <div class="flex items-center gap-3">
                <div class="h-10 w-10 flex-shrink-0 rounded-full bg-indigo-100 text-indigo-700 flex items-center justify-center font-semibold">
                  <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                  </svg>
                </div>
                <div class="min-w-0">
                  <p class="text-sm font-semibold text-gray-900">Estimated duration</p>
                  <p class="text-sm text-gray-600">
                    <% const totalMins = Math.round(stats.durationSeconds / 60); const hours = Math.floor(totalMins / 60); const mins = totalMins % 60; %>
                    <%= hours > 0 ? `${hours}h ${mins}m` : `${mins} min` %> (<%= Math.round(stats.durationSeconds) %>s)
                  </p>
                </div>
              </div>
              <% if (stats.note) { %>
                <span class="text-xs text-gray-400 sm:text-right"><%= stats.note %></span>
              <% } %>
            </div>
          <% } %>

          <% if (stats.speakers && stats.speakers.length > 0) { %>
            <div>
              <h4 class="text-sm font-semibold text-gray-900 mb-3">Talk time by speaker</h4>
              <div class="space-y-2">
                <% stats.speakers.forEach(function(speaker) { %>
                  <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-3 rounded-lg border border-gray-100 p-3">
                    <div class="min-w-0 flex-1">
                      <p class="text-sm font-medium text-gray-900 truncate"><%= speaker.name || 'Speaker' %></p>
                      <p class="text-xs text-gray-500">
                        <% const secs = Math.round(speaker.talkTimeSeconds || 0); const mins = Math.floor(secs / 60); const rem = secs % 60; %>
                        <%= mins > 0 ? `${mins}m ${rem}s` : `${secs}s` %>
                        <% if (speaker.turns) { %>
                          · <%= speaker.turns %> turns
                        <% } %>
                        <% if (speaker.wordCount) { %>
                          · <%= speaker.wordCount %> words
                        <% } %>
                      </p>
                    </div>
                    <div class="flex items-center gap-3 flex-shrink-0">
                      <% if (speaker.talkTimePercent !== null && speaker.talkTimePercent !== undefined) { %>
                        <span class="text-sm font-semibold text-indigo-700 w-12 text-right"><%= speaker.talkTimePercent.toFixed(1) %>%</span>
                      <% } %>
                      <div class="w-24 sm:w-32 bg-gray-100 rounded-full h-2 overflow-hidden">
                        <% const pct = Math.max(0, Math.min(100, speaker.talkTimePercent || 0)); %>
                        <div class="h-2 bg-indigo-500" style="width:<%= pct %>%"></div>
                      </div>
                    </div>
                  </div>
                <% }); %>
              </div>
            </div>
          <% } %>
        </div>
      <% } else { %>
        <div class="text-center py-8">
          <svg class="mx-auto h-12 w-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 3.055A9.001 9.001 0 1020.945 13H11V3.055z"></path>
          </svg>
          <h3 class="mt-2 text-sm font-semibold text-gray-900">Stats unavailable</h3>
          <p class="mt-1 text-sm text-gray-500">Talk-time stats will appear here once transcript timing is processed.</p>
        </div>
      <% } %>
    </div>

    <!-- Media Tab (video/audio recording) -->
    <div id="content-media" class="tab-content hidden" role="tabpanel" aria-labelledby="tab-media">
      <% if (meeting.videoUrl || meeting.audioUrl || meeting.teamsRecordingUrl) { %>
        <%- include('recording', { meeting: meeting }) -%>
      <% } else { %>
        <div class="text-center py-12">
          <svg class="mx-auto h-12 w-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 10l4.553-2.276A1 1 0 0121 8.618v6.764a1 1 0 01-1.447.894L15 14M5 18h8a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v8a2 2 0 002 2z"></path>
          </svg>
          <h3 class="mt-2 text-sm font-semibold text-gray-900">No media yet</h3>
          <p class="mt-1 text-sm text-gray-500">Video or audio will appear here once the recording is available.</p>
        </div>
      <% } %>
    </div>

    <!-- Quick Actions Tab -->
    <div id="content-quickactions" class="tab-content hidden" role="tabpanel" aria-labelledby="tab-quickactions">
      <%- include('tab-quickactions', { meeting }) -%>
    </div>
  </div>
</div>
