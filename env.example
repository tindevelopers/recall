# Recall Calendar Integration - Environment Variables
# Copy this file to .env and fill in your values
# DO NOT commit .env to version control

# =============================================================================
# REQUIRED VARIABLES
# =============================================================================

# JWT Secret Key - Generate with: openssl rand -hex 32
SECRET=your-jwt-secret-key-here

# Recall API Configuration
RECALL_API_KEY=your-recall-api-key-here
RECALL_API_HOST=https://api.recall.ai

# Public URL - Your application's public URL (e.g., https://your-app.railway.app)
PUBLIC_URL=https://your-app.railway.app

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# PostgreSQL Database URL
# Format: postgresql://user:password@host:port/database
# Railway automatically sets this when you add a PostgreSQL service
DATABASE_URL=postgresql://user:password@localhost:5432/recall

# =============================================================================
# REDIS CONFIGURATION
# =============================================================================

# Redis URL for background job queue
# Railway automatically sets this when you add a Redis service
# Local development: redis://127.0.0.1:6379
REDIS_URL=redis://127.0.0.1:6379

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Server Port (default: 3003)
PORT=3003

# Node Environment (development | production)
NODE_ENV=development

# =============================================================================
# OAUTH CONFIGURATION (Optional - for calendar integrations)
# =============================================================================

# Google Calendar OAuth
GOOGLE_CALENDAR_OAUTH_CLIENT_ID=your-google-oauth-client-id
GOOGLE_CALENDAR_OAUTH_CLIENT_SECRET=your-google-oauth-client-secret

# Microsoft Outlook OAuth
MICROSOFT_OUTLOOK_OAUTH_CLIENT_ID=your-microsoft-oauth-client-id
MICROSOFT_OUTLOOK_OAUTH_CLIENT_SECRET=your-microsoft-oauth-client-secret

# =============================================================================
# LLM CONFIGURATION (Optional - for AI features)
# =============================================================================

# LLM Provider (openai | anthropic)
LLM_PROVIDER=openai

# OpenAI Configuration
OPENAI_API_KEY=your-openai-api-key
OPENAI_MODEL_SUMMARY=gpt-4-turbo
OPENAI_MODEL_EMBEDDINGS=text-embedding-3-small
# Optional: Control AI summarization quality
# OPENAI_TEMPERATURE=0.3  # Lower = more focused/factual (default: 0.3)
# OPENAI_MAX_TOKENS=4000  # Maximum tokens for summaries (default: 4000)

# Anthropic Configuration
ANTHROPIC_API_KEY=your-anthropic-api-key
ANTHROPIC_MODEL=claude-3-haiku-20240307

# AssemblyAI Configuration (for LLM Gateway)
ASSEMBLYAI_API_KEY=your-assemblyai-api-key
# Super Agent (AssemblyAI premium): set to true/1/yes/on/enabled to enable globally, or enable per calendar in Settings → Bot Settings
# SUPER_AGENT_ENABLED=true

# LLM Model Selection
LLM_SIMPLE_MODEL=gpt-3.5-turbo
LLM_COMPLEX_MODEL=gpt-4-turbo

# =============================================================================
# RAILWAY-SPECIFIC (Auto-set by Railway, optional for local dev)
# =============================================================================

# Railway automatically sets these when services are added:
# - DATABASE_URL (from PostgreSQL service)
# - REDIS_URL (from Redis service)
# - RAILWAY_PUBLIC_DOMAIN (from deployment)
# - RAILWAY_STATIC_URL (from deployment)

# =============================================================================
# NOTES
# =============================================================================
#
# 1. Required Variables:
#    - SECRET: Generate with: openssl rand -hex 32
#    - RECALL_API_KEY: Get from https://recall.ai
#    - RECALL_API_HOST: Usually https://api.recall.ai
#    - PUBLIC_URL: Your deployed application URL
#
# 2. Database:
#    - For Railway: DATABASE_URL is auto-set when you add PostgreSQL
#    - For local dev: Use a local PostgreSQL instance or Railway's local proxy
#
# 3. Redis:
#    - For Railway: REDIS_URL is auto-set when you add Redis
#    - For local dev: Use local Redis or Railway's local proxy
#
# 4. OAuth:
#    - Only needed if you want Google Calendar or Microsoft Outlook integration
#    - Set up OAuth apps in Google Cloud Console / Azure Portal
#
# 5. LLM:
#    - Only needed for AI features (summaries, embeddings, chat)
#    - OpenAI is used by default if OPENAI_API_KEY is set
#    - Anthropic can be used by setting LLM_PROVIDER=anthropic
#
# =============================================================================

